using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class carlightsscript : MonoBehaviour
{
    bool lights = true;
    // Start is called before the first frame update
    /*void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
   /* private void OnMouseDown()
    {
        if (!lights)
        {
            lights = true;
            this.transform.localPosition = this.transform.localPosition - new Vector3(0f, 0f, 0.02f);
            teicarcontroller.headlightsopen();
        }
        else
        {
            lights = false;
            this.transform.localPosition = this.transform.localPosition + new Vector3(0f, 0f, 0.02f);
            teicarcontroller.headlightsclose();
        }

    }
    private void OnMouseEnter()
    {
        usepanelactive.takepanel.SetActive(true);
        usepanelactive.whichobject.text = "Lights";
    }
    private void OnMouseExit()
    {
        usepanelactive.takepanel.SetActive(false);
        usepanelactive.whichobject.text = "";
    }*/
}
